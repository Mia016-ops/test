# 触发条件：当 Issue 被编辑（包括标签更改）时
on:
  issues:
    types: [labeled, unlabeled]

jobs:
  send_email_on_resolved:
    # 仅在 Issue 标签包含 'resolved' 且 'unlabeled' 事件未发生时运行
    if: |
      github.event.action == 'labeled' &&
      contains(github.event.issue.labels.*.name, 'resolved')
    runs-on: ubuntu-latest
    
    steps:
        # 1. 提取收件人邮箱
      - name: 📩 Extract Email from Issue Body (Conditional)
        id: extract_email
        run: |
          ISSUE_BODY="${{ github.event.issue.body }}"
  
          LINE_FROM=$(echo "$ISSUE_BODY" | sed -n '2p')
          LINE_REPLY_TO=$(echo "$ISSUE_BODY" | sed -n '3p')
  
          TARGET_LINE=""
  
          # 检查 LINE_FROM 是否包含 "support@filomail.com" 
          if echo "$LINE_FROM" | grep -iq "Support"; then
            TARGET_LINE="$LINE_REPLY_TO"
            echo "Info: 'From' field contains 'support@filomail.com'. Targeting 'Reply-To' line (Line 3)."
          else
            TARGET_LINE="$LINE_REPLY_TO"
            echo "Info: 'From' field does not contain 'support@filomail.com'. Targeting 'From' line (Line 3)."
          fi
  
          EMAIL_REGEX='([a-zA-Z0-9._-]+@[a-zA-Z0-9._-]+\.[a-zA-Z0-9_-]+)'
  
          # 从目标行中提取邮件
          email=$(echo "$TARGET_LINE" | grep -o -E $EMAIL_REGEX)
          
          # 检查结果并设置输出变量
          if [ -z "$email" ]; then
            echo "::error::Could not find email address in the target line: $TARGET_LINE. Skipping email."
            echo "extracted_email=no-reply@example.com" >> "$GITHUB_OUTPUT"
          else
            echo "extracted_email=$email" >> "$GITHUB_OUTPUT"
          fi
        shell: bash
      
      # 2. 发送 Resolved 通知邮件
      - name: ✉️ Send Resolved Notification Email
        uses: dawidd6/action-send-mail@v3
        # 仅在成功提取到邮箱后才运行
        if: success() && steps.extract_email.outputs.extracted_email != 'no-reply@example.com'
        with:
          # ... (SMTP 配置不变) ...
          server_address: ${{ secrets.SMTP_HOST }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          from: ${{ secrets.MAIL_FROM }}
          
          # 使用上一步骤提取的邮箱作为收件人
          to: ${{ steps.extract_email.outputs.extracted_email }}
          
          # ... (邮件内容配置不变) ...
          subject: "[Resolved] Issue # ${{ github.event.issue.number }}: ${{ github.event.issue.title }}"
          html_body: |
            <p>尊敬的工单提交人，</p>
            <p>您提交的 GitHub Issue 状态已更新：</p>
            <ul>
              <li><strong>Issue 标题:</strong> <a href="${{ github.event.issue.html_url }}">${{ github.event.issue.title }}</a></li>
              <li><strong>Issue 编号:</strong> #${{ github.event.issue.number }}</li>
              <li><strong>新状态:</strong> <strong>已解决 (Resolved)</strong></li>
            </ul>
            <p><strong><a href="${{ github.event.issue.html_url }}">点击此处查看 Issue 详情</a></strong></p>
            <br>
            <p>请注意：此邮件由 GitHub Actions 自动化发送，请勿直接回复。</p>
